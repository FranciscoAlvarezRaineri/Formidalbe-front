import { createElement } from "react";

// specify the inputs required for a string type object
export default function CardDefaultParameterInputs({ parameters, onChange }) {
  return createElement("div", null);
}
